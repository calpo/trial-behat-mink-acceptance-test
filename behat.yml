default:
  extensions:
    Behat\MinkExtension\Extension:
      default_session: goutte
      base_url: 'http://oshiete.goo.ne.jp/'
      goutte:    ~
  paths:
    features:  features
    bootstrap: features/bootstrap

annotations:
  paths:
    features: features/annotations

closures:
  paths:
    features: features/closures
